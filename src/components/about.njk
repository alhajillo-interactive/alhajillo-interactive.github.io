{% set data = about[locale] %}
<div class="container">
    <h2>{{ data.title }}</h2>
    <p>{{ data.description}}</p>
    {# <h3>Team</h3>
    <ul>
        <li>
            <h4>Bryan</h4>
        </li>
        <li>
            <h4>Isma</h4>
        </li>
        <li>
            <h4>Kale</h4>
        </li>
        <li>
            <h4>Serghy</h4>
        </li>
    </ul> #}
    <h3>{{data.our_studio.title}}</h3>
    <p>{{data.our_studio.mission}}</p>
    {% for highlight in data.our_studio.highlights %}
    <p>> {{highlight}}</p>
    {% endfor %}
</div>
<div class="container">
    <h2>{{data.services.title}}</h2>
    <ul>
        {% for service in data.services.items %}
        <li>{{service}}</li>
        {% endfor %}
    </ul>
</div>
<div class="container">
    <div class="cta-btns">
        <h3>{{data.cta.title}}</h3>
        <a class="cta-btn" href="#portfolio">{{data.cta.projects_btn}}</a>
        <a class="cta-btn" href="#contact-us">{{data.cta.contact_btn}}</a>
    </div>
</div>
